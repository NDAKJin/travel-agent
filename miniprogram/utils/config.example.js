// Copy this file to config.js and set the backend address for your environment.
// The copied config.js is intentionally ignored by Git.
const BASE_URL = "https://your-api.example.com";

module.exports = {
  BASE_URL
};
