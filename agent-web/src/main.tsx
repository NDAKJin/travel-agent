import { StrictMode, useState } from "react";
import { createRoot } from "react-dom/client";
import "./style.css";
const API = import.meta.env.VITE_API_BASE_URL ?? "";
async function post<T>(path: string, body: unknown): Promise<T> { const r = await fetch(`${API}${path}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); if (!r.ok) throw new Error(await r.text()); return r.json(); }
function App() { const [email,setEmail]=useState(""); const [code,setCode]=useState(""); const [logged,setLogged]=useState(false); const [input,setInput]=useState(""); const [reply,setReply]=useState(""); const [sessionId,setSessionId]=useState<string|null>(null); const [tip,setTip]=useState(""); const send=async()=>{try{await post("/api/auth/email/send-code",{email});setTip("Code sent")}catch(e){setTip(String(e))}}; const login=async()=>{const r=await post<{valid:boolean}>("/api/auth/email/verify-code",{email,code});if(r.valid)setLogged(true);else setTip("Invalid code")}; const chat=async()=>{const r=await post<{sessionId:string;reply:string}>("/api/agent/chat",{message:input,sessionId});setSessionId(r.sessionId);setReply(r.reply);setInput("")}; return logged?<main className="chat"><header><b>TRAVEL AGENT</b><h1>Your travel assistant</h1></header><section className="conversation">{reply&&<div className="bubble">{reply}</div>}</section><footer><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Tell me your travel needs" onKeyDown={e=>e.key==="Enter"&&void chat()}/><button onClick={()=>void chat()}>Send</button></footer></main>:<main className="login"><h1>Travel Agent</h1><p>Sign in to plan your trip</p><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><div><input placeholder="Verification code" value={code} onChange={e=>setCode(e.target.value)}/><button onClick={()=>void send()}>Send code</button></div><button className="primary" onClick={()=>void login()}>Sign in / Register</button><small>{tip}</small></main>; }
createRoot(document.getElementById("root")!).render(<StrictMode><App/></StrictMode>);
